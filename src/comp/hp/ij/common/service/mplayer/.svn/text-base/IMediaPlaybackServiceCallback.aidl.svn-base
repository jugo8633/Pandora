package comp.hp.ij.common.service.mplayer;

oneway interface IMediaPlaybackServiceCallback {
    /**
     * Called when the service has a new value for you.
     */
   
    // call back when one song complete
    void onComplete(int complete_status);
    
    // call back when error occure
    void onErrorReport(int error_code); 
}
